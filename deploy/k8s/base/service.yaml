apiVersion: v1
kind: Service
metadata:
  name: voxcpm-space
  labels:
    app.kubernetes.io/name: voxcpm-space
    app.kubernetes.io/part-of: voxcpm-space
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: voxcpm-space
    app.kubernetes.io/part-of: voxcpm-space
  ports:
    - name: http
      port: 80
      targetPort: 3000
